<?php

function groupByOwners(array $files) : array
{
    $result = array();
    foreach ($files as $value=>$key) {
    	
    }
    return $trait_uses_recursive($trait);
}

$files = array
(
    "Input.txt" => "Randy",
    "Code.py" => "Stan",
    "Output.txt" => "Randy"
);
var_dump(groupByOwners($files));